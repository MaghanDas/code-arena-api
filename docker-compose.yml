version: "3.9"

services:
  laravel:
    build: .

    container_name: code-arena-api

    ports:
      - "8000:80"

    env_file:
      - .env

    volumes:
      - .:/var/www

    restart: unless-stopped